# Disable URL rewriting for the installer directory.
# This prevents the main .htaccess trailing-slash redirect from causing
# an infinite redirect loop when accessing /install/.
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Prevent directory listing
Options -Indexes
